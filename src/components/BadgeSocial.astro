---
const { class: className, href, title } = Astro.props;
---

<a
    class={`${className} flex items-center gap-1.5 py-1.5 px-3 bg-white/5 border border-white/10 rounded-full text-sm backdrop-blur-sm hover:scale-105 hover:bg-white/10 transition-all duration-300 ease-in-out`}
    target="_blank"
    href={href}
    rel="noreferrer"
    title={title}
>
    <slot />
</a>
